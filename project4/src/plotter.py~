import numpy as np
import matplotlib.pyplot as plt

def plot_mean(filename_energy, filename_mag):
    mean_energy = np.loadtxt(filename_energy)
    mean_mag = np.loadtxt(filename_mag)

    print(mean_energy)
    print "HEIEH"
    print(mean_energy[:,0])

plot_mean("data_expectationE_temp1.txt", "data_expectationE_temp24")
